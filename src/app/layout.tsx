import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "TheHoroscope.co - Daily Horoscope and Zodiac Signs",
  description: "Get your daily horoscope, zodiac sign compatibility, and astrology insights. Learn about love, career, and health based on your star sign.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
